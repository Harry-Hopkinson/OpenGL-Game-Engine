#include <src/core/render/init.cpp>
// render a image to the screen
// with a function called renderImage
// that takes in a path to the image
// and a position to render the image at
// and a size to render the image at
// and a rotation to render the image at

int renderImage(std::string imagePath, int x, int y, int width, int height, int rotation) {
  init();
  return 0;
}